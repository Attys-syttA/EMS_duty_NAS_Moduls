# PowerShell helper for sending watchdog event
# Usage (PowerShell):
#   python .\EMS_Duty_Moduls\scripts\send_watchdog_event.py --action restart --reason "hotfix"

# The script is intentionally minimal because the actual helper is a Python script.
Write-Output "Use the Python helper: python .\EMS_Duty_Moduls\scripts\send_watchdog_event.py --action restart --reason 'hotfix'"
